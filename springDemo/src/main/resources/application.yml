server:
  port: 8080


spring:
  application:
    name: yesbuter-backend

management:
  endpoints:
    web:
      exposure:
        include=*:

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8024/eureka/